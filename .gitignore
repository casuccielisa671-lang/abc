# IDE
.idea/
*.iml
.vscode/
.codebuddy/
.mybatis/

# 运行时生成物（报告服务输出）
# 不加前导斜杠：报告落盘位置取决于进程工作目录 ——
# `mvn spring-boot:run -pl occupation-web` 会写到 occupation-web/data/reports/，
# 只写 `data/reports/` 时那个路径不会被忽略，二进制报告会被误提交
data/reports/
**/data/reports/
# 证件照上传目录（同理，取决于工作目录，二进制图片不入库）
data/avatars/
**/data/avatars/

# 本机私密配置（AI 密钥等）。仓库里只留 application-local.yml.example
application-local.yml

# Maven
target/
*.jar
*.war

# Node
node_modules/
dist/
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Docker
docker-compose.override.yml

# Temp
*.tmp
*.swp
*.swo
